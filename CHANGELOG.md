# Changelog

## [1.0.0]

### Features

- Binary `Compress` / `Decompress` for primitives, tables (arrays, dicts,
  nested, shared references, cycles) and every Roblox value type.
- Optional schemas with defaults, validation, and smaller output.
- Custom type registration, instance path mode, base64 helpers for
  DataStores, tunable options with strict mode.